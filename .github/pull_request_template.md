## Summary

- 

## Folia safety checklist

- [ ] Entity, player, inventory, and GUI state runs on the entity scheduler.
- [ ] Block reads and writes run on the owning region scheduler.
- [ ] Chunk loading uses async APIs.
- [ ] Teleports use `teleportAsync`.
- [ ] New visible text is configurable.
- [ ] Build passes with `./gradlew build`.
